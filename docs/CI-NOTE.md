# Contract validation

The `Validate contracts` GitHub Actions workflow installs pinned dependencies, compiles Solidity 0.8.28, and runs the complete Hardhat test suite on every push to `main` and every pull request.
